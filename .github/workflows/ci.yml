name: CI

on:
    push:
      branches: [ "main" ]
    pull_request:
      branches: [ "main" ]
    workflow_dispatch:
      inputs: {}

jobs:
    ci:
        runs-on: linux
        name: Build and test
        steps:
            -  uses: actions/checkout@v3
            -  name: Use Node 20
               uses: actions/setup-node@v4
               with:
                node-version: '20.x'
            -  run: pnpm install
            -  run: pnpm build
            -  run: pnpm check-types
            -  run: pnpm test